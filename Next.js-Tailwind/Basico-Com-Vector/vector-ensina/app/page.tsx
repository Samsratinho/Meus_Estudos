export default function Sams(){
  
  return(
    <h1 className="text-red-500">oi</h1>
  )
}

/* "use client"

import { useState } from "react"

export default function Home() {

  const [num, setNum] = useState<number>(0)

  function addPlusOne(){
    setNum(num+1)
  }

  return (
    <div className="flex items-center justify-center w-screen h-screen flex-col gap-5">
      <h1 className="text-5xl font-semibold">Teste de Sams</h1>
      <h1 className="font-semibold text-lg">{num}</h1>
      <button onClick={addPlusOne} className="cursor-pointer bg-blue-500 px-5 py-2 rounded-full">Add +1</button>
    </div>
  );
}
 */